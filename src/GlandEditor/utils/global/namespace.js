const editorName = 'GEditor';
const editorStyleTagId = `${editorName}_private`;
const editorCoreClassName = `${editorName}_core`;
const editEvent = `${editorName}_editEvent`;
const preEditEvent = `${editorName}_preEditEvent`;

export {editorName, editorStyleTagId, editorCoreClassName, editEvent, preEditEvent};
